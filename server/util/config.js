
export const FETCH_OPTIONS = {
  cache: 'no-cache', // *default, no-cache, reload, force-cache, only-if-cached
  headers: {
    'Content-Type': 'application/json',
  },
};

const CONFIG = { FETCH_OPTIONS };

export default CONFIG;
