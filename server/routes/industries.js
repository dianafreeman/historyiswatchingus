import { STATIC_DATA } from '../constants';

const getIndustry = (params) => {
  console.log(STATIC_DATA.inds.find(params));
//   return 'true';
//   STATIC_DATA.ins.map((i) => {
//     console.log(i.code);
//     //   let vals = Object.values(i);
//     //   return vals.find(params)
//   });
};

export default getIndustry;
