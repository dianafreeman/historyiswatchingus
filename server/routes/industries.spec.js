import { expect } from 'chai';
import { describe, it } from 'mocha';
import getIndustry from './industries';

describe('Industries ', () => {
//   getIndustry({name: 'Fuel oil dealers'});
    // const expected = 'string';
    // expect(test).to.equal(expected);
});
